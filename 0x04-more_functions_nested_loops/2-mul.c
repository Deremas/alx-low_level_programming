#include "main.h"

/*
 * mul - multiplies two numbers
 * @a: the first number to multiplly
 * @b: the second number to multiply with the first number
 * Return: the result that got by multiplication of a and b
 */

int mul(int a, int b)


{
	return (a * b);

}
